- FTP anonymous check

```
ftp 10.10.10.123
```

- Then provide `anonymous` in credentials, to check if the FTP server is vulnerable to the anonymous login vulnerability, if it works, you have FTP access, then look for valuable files especially .txt files.