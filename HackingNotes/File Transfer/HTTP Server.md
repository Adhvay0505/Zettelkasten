my go-to command to spawn a HTTP server to send something from my attacker machine to victim machine,
after gaining a shell on it, or for sending anything from host to VM's

```python
python3 -m http.server 8080
```

